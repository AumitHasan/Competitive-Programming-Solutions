#include<stdio.h>
int main(){

    int n;
    scanf("%d",&n);
    int a[n];

    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
        dif=a[0]-a[1];
        if(dif<0)
            dif*=-1;
        if(i>0 && c==0)
        {
            x=a[i]-a[i-1];
            if(x<0)
                x*=-1;
            i

        }
    }



return 0;
}
